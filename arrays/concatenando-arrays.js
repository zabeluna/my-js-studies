// [] = array ||| {} = objeto

//For concatenation of arrays, I can use two methods: ...[] or arrayName.concat(array).

const array1 = ['ana', 'maria', 'caio'];
const array2 = ['matt', 'ray', 'oceane'];
const arrayConcatenando = [...array1, ...array2];
console.log(arrayConcatenando);
